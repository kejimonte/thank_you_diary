<div class="text-center mb-4">
  <h2>ログイン</h2>
</div>

<div class="devise-form">
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

    <div class="mb-3 text-center">
      <%= f.label :email, "メールアドレス", class: "form-label" %>
      <%= f.email_field :email, autofocus: true, class: "form-control" %>
    </div>

    <div class="mb-3 text-center">
      <%= f.label :password, "パスワード", class: "form-label" %>
      <%= f.password_field :password, class: "form-control" %>
    </div>

    <% if devise_mapping.rememberable? %>
      <div class="mb-3 text-center">
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me, "ログイン状態を保持する" %>
      </div>
    <% end %>

    <%= f.submit "ログイン", class: "btn btn-primary w-100 mt-3" %>


  <% end %>
</div>

<div class="login-container">
  <%= link_to user_google_oauth2_omniauth_authorize_path, method: :post, data: {turbo: false}, class: "google-btn", id: "google-login-btn" do %>
  <%= image_tag "google_logo.svg", class: "google-icon" %>
    <span>Googleでログイン</span>
  <% end %>
  <%= link_to user_line_omniauth_authorize_path, class: "google-btn", style: "background-color: #06C755; color: white;" do %>
    <img src="https://upload.wikimedia.org/wikipedia/commons/4/41/LINE_logo.svg" width="20">
    <span>LINEでログイン</span>
  <% end %>
</div>
<div class="text-center mt-3">
  <%= render "devise/shared/links" %>
</div>
